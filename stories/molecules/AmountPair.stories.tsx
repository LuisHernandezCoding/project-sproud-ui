import React from 'react';
import AmountPair from '../../molecules/AmountPair.tsx';

export default {
  title: 'Generated/Molecules/AmountPair',
  component: AmountPair,
  // Automatically infer controls from component props if using Args
};

const Template = (args) => <AmountPair {...args} />;

export const Primary = Template.bind({});
Primary.args = {};
