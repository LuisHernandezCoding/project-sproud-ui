import React from 'react';
import RecentActivity from '../../molecules/RecentActivity.tsx';

export default {
  title: 'Generated/Molecules/RecentActivity',
  component: RecentActivity,
  // Automatically infer controls from component props if using Args
};

const Template = (args) => <RecentActivity {...args} />;

export const Primary = Template.bind({});
Primary.args = {};
