import React from 'react';
import ResponsiveContent from '../../ResponsiveContent/ResponsiveContent.tsx';

export default {
  title: 'Generated/Others/ResponsiveContent',
  component: ResponsiveContent,
  // Automatically infer controls from component props if using Args
};

const Template = (args) => <ResponsiveContent {...args} />;

export const Primary = Template.bind({});
Primary.args = {};
